name: Build and Deploy Auth Service

on:
  push:
    branches:
      - main
    paths:
      - 'auth-service/**'

jobs:
  build-deploy:
    name: Build and Deploy Auth Service
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: 21
          distribution: 'corretto'
          cache: 'maven'

      - name: Unit Tests
        run: mvn -B test --file auth-service/pom.xml

      - name: Build the application
        run: mvn -B clean package --file auth-service/pom.xml -DskipTests

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v6
        with:
          context: ./auth-service
          push: true
          file: ./auth-service/Dockerfile
          tags: |
            ${{ secrets.DOCKER_HUB_USERNAME }}/auth-service:${{ github.sha }}
            ${{ secrets.DOCKER_HUB_USERNAME }}/auth-service:latest